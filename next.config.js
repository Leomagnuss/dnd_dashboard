/** @type {import('next').NextConfig} */
const nextConfig = {
  // Разрешаем подключения с твоего сервера в режиме разработки
  allowedDevOrigins: ['2.27.51.159', 'localhost'],
}

module.exports = nextConfig
